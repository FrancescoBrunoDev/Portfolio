import { Outfit, Playfair_Display, Rubik } from "next/font/google";

export const outfit = Rubik({ subsets: ["latin"] });
export const elegant = Playfair_Display({
  subsets: ["latin"],
  display: "swap",
});

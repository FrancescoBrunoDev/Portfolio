diff --git a/node_modules/next-mdx-remote/dist/index.js b/node_modules/next-mdx-remote/dist/index.js
index 302ffff..bd2e389 100644
--- a/node_modules/next-mdx-remote/dist/index.js
+++ b/node_modules/next-mdx-remote/dist/index.js
@@ -3,7 +3,7 @@
  * SPDX-License-Identifier: MPL-2.0
  */
 import './idle-callback-polyfill.js';
-import React, { useEffect, useState, useMemo } from 'react';
+import { createElement, useEffect, useState, useMemo } from 'react';
 import { jsxRuntime } from './jsx-runtime.cjs';
 import * as mdx from '@mdx-js/react';
 /**
@@ -39,12 +39,12 @@ export function MDXRemote({ compiledSource, frontmatter, scope, components = {},
     }, [scope, compiledSource]);
     if (!isReadyToRender) {
         // If we're not ready to render, return an empty div to preserve SSR'd markup
-        return (React.createElement("div", { dangerouslySetInnerHTML: { __html: '' }, suppressHydrationWarning: true }));
+        return (createElement("div", { dangerouslySetInnerHTML: { __html: '' }, suppressHydrationWarning: true }));
     }
     // wrapping the content with MDXProvider will allow us to customize the standard
     // markdown components (such as "h1" or "a") with the "components" object
-    const content = (React.createElement(mdx.MDXProvider, { components: components },
-        React.createElement(Content, null)));
+    const content = (createElement(mdx.MDXProvider, { components: components },
+        createElement(Content, null)));
     // If lazy = true, we need to render a wrapping div to preserve the same markup structure that was SSR'd
-    return lazy ? React.createElement("div", null, content) : content;
+    return lazy ? createElement("div", null, content) : content;
 }
